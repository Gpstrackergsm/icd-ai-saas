import MapView, { Marker as NativeMarker, Polyline as NativePolyline, PROVIDER_GOOGLE as NativeProvider } from 'react-native-maps';

export const Marker = NativeMarker;
export const Polyline = NativePolyline;
export const PROVIDER_GOOGLE = NativeProvider;
export default MapView;
